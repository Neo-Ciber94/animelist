{
  "name": "root",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.3.0",
    "rimraf": "^5.0.1"
  },
  "scripts": {
    "build": "npx lerna run build",
    "clean": "rimraf dist",
    "typecheck": "npx lerna run typecheck"
  }
}